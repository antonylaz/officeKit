{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma migrate deploy && next build",
  "framework": "nextjs",
  "regions": ["arn1"],
  "crons": [
    {
      "path": "/api/v1/cron/sync-feeds",
      "schedule": "0 3 * * *"
    }
  ]
}
